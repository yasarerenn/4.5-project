spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: 
          default-label: master
          search-paths: 
            - configurations/
            - configurations/{application}

server:
  port: 8787