spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yasarerenn/4.5-project
          default-label: main
          search-paths: 
            - configurations/
            - configurations/{application}

server:
  port: 8787